# Phase 2: 向量与拼装 (VCN/LCN) 执行计划

**阶段**: Phase 2 - 向量库集成 + 记忆拼装  
**目标**: 解决长文本记忆的切片与还原问题  
**周期**: 4-6 周 (2026-04-14 ~ 2026-05-12)  
**开发模式**: TDD (延续 Phase 1)  

---
## 一、Phase 2 核心目标

```
在 Phase 1 (单机版 SQLite + MCP) 基础上，引入：
1. 文本自动切片策略 (按语义/长度/段落)
2. 向量库集成 (ChromaDB 本地模式)
3. MFT 指针关联 (lcn_pointers 字段)
4. Assembler 动态拼装算法
5. 性能优化 (LRU 缓存 + 并行捞取)
```

---
## 二、验收标准

| 指标 | 目标值 | 测试方法 |
|------|-------|---------|
| **功能** | 写入 5000 字文档，能精准召回不同段落 | 集成测试 |
| **功能** | 拼装后文本逻辑连贯 | 人工评估 (>85% 可接受) |
| **性能** | 读取延迟 < 200ms (含拼装) | 基准测试 |
| **性能** | 写入延迟 < 500ms (含切片 + 向量化) | 基准测试 |
| **代码** | 单元测试覆盖率 > 80% | pytest --cov |
| **文档** | 5 篇新文档 + 更新现有文档 | 人工审查 |

---
## 三、详细任务拆解

### Task 1: 文本自动切片策略 (Week 1)

**负责人**: OMO Agent  
**监督**: main  
**预计时间**: 5-7 天  

**子任务**:
```
1.1 切片规则设计
    - 按段落切分 (自然段落边界)
    - 按长度切分 (单片 500-2000 字)
    - 按语义切分 (嵌入向量相似度断点)
    - 重叠切分 (相邻切片保留 10-20% 重叠)

1.2 切片器实现
    - ParagraphSplitter (段落切分)
    - LengthSplitter (长度切分)
    - SemanticSplitter (语义切分，需向量支持)
    - HybridSplitter (混合策略)

1.3 单元测试
    - 测试边界情况 (空文本、超长段落、特殊字符)
    - 测试重叠率准确性
    - 测试切片元数据 (位置、长度、父节点)
```

**交付物**:
- `mfs/slicers/__init__.py`
- `mfs/slicers/paragraph.py`
- `mfs/slicers/length.py`
- `mfs/slicers/semantic.py`
- `mfs/slicers/hybrid.py`
- `tests/test_slicers.py`

**验收标准**:
- 5000 字文档切分成 5-15 个切片
- 单一切片长度 500-2000 字 (允许±10% 弹性)
- 相邻切片重叠率 10-20%
- 切片后能还原原文 (无信息丢失)
- 测试覆盖率 > 80%

---
### Task 2: 向量库集成 (Week 2-3)

**负责人**: OMO Agent  
**监督**: main  
**预计时间**: 10-14 天  

**子任务**:
```
2.1 技术选型确认
    - 向量库：ChromaDB (本地模式，轻量级)
    - 向量模型：BGE-M3 (中文友好，1024 维)

2.2 向量库封装
    - VectorStore 类实现
    - 支持添加/删除/搜索
    - 支持批量操作
    - 支持元数据过滤

2.3 与 MFT 集成
    - 写入记忆时自动计算向量
    - 搜索时支持语义检索
    - 向量索引与 MFT inode 关联
```

**交付物**:
- `mfs/vector_store.py`
- `mfs/embedding.py`
- `tests/test_vector_store.py`
- `docs/VECTOR_INTEGRATION.md`

**验收标准**:
- 向量搜索延迟 < 50ms (千级数据量)
- 搜索准确率 > 85% (人工评估)
- 支持元数据过滤 (按 path/type/status 过滤)
- 测试覆盖率 > 80%

---
### Task 3: MFT 指针关联 (Week 3)

**负责人**: OMO Agent  
**监督**: main  
**预计时间**: 5-7 天  
**调整原因**: 涉及写入/读取流程改造 + 两阶段提交，工作量比预期大  

**子任务**:
```
3.1 lcn_pointers 字段设计
    - JSON 格式存储指针列表
    - 每个指针包含：chunk_id, offset, length, vector_id
    - 支持动态扩展

3.2 写入流程改造
    - mfs_write 接收长文本 → 自动切片 → 写入向量库 → 更新 MFT
    - 两阶段提交 (先写 Chunk，再更新 MFT)

3.3 读取流程改造
    - mfs_read 读取 MFT → 解析 lcn_pointers → 捞取 Chunk → 拼装返回
    - 支持部分读取 (只读取特定 Chunk)

3.4 单元测试
    - 测试指针关联准确性
    - 测试并发写入 (防冲突)
    - 测试指针清理 (删除记忆时同步删除 Chunk)
```

**交付物**:
- `mfs/mft.py` (更新 lcn_pointers 字段)
- `mfs/chunk_manager.py` (新增)
- `tests/test_chunk_manager.py`

**验收标准**:
- 写入 5000 字文档，MFT 正确关联 5-15 个 Chunk
- 读取时能精准召回所有关联 Chunk
- 删除记忆时，关联 Chunk 同步清理 (无孤儿数据)
- 测试覆盖率 > 80%

---
### Task 4: Assembler 动态拼装 (Week 4-5)

**负责人**: OMO Agent  
**监督**: main  
**预计时间**: 10-14 天  

**子任务**:
```
4.1 拼装策略设计
    - 按时间戳排序 (默认)
    - 按语义相似度排序 (向量距离)
    - 按用户自定义规则 (如优先级)

4.2 拼装器实现
    - Assembler 类实现
    - 支持多种排序策略
    - 支持重叠部分去重
    - 支持逻辑连贯性检查

4.3 边界情况处理
    - 缺失 Chunk 处理 (部分数据损坏)
    - 冲突 Chunk 处理 (同一位置多个版本)
    - 超长文本拼装 (内存优化)

4.4 单元测试
    - 测试拼装准确性
    - 测试去重逻辑
    - 测试边界情况 (空数据、单一切片、损坏数据)
```

**交付物**:
- `mfs/assembler.py`
- `tests/test_assembler.py`
- `docs/ASSEMBLER.md`

**验收标准**:
- 拼装后文本逻辑连贯 (人工评估 > 85% 可接受)
- 重叠部分正确去重 (无重复内容)
- 缺失 Chunk 时有明确错误提示
- 测试覆盖率 > 80%

---
### Task 5: 性能优化 (Week 5)

**负责人**: OMO Agent  
**监督**: main  
**预计时间**: 5-7 天  

**子任务**:
```
5.1 LRU 缓存实现
    - 缓存热点 MFT 记录
    - 缓存热点 Chunk 数据
    - 缓存向量搜索结果

5.2 并行捞取优化
    - 多线程/异步并发捞取 Chunk
    - 批量向量搜索优化

5.3 数据库优化
    - SQLite 索引优化
    - WAL 模式开启
    - 连接池管理

5.4 性能基准测试
    - 读写延迟测试
    - 并发测试
    - 大数据量测试 (万级记录)
```

**交付物**:
- `mfs/cache.py`
- `tests/test_cache.py`
- `benchmarks/` (性能测试脚本)
- `docs/PERFORMANCE.md`

**验收标准**:
- 读取延迟 < 200ms (含拼装)
- 写入延迟 < 500ms (含切片 + 向量化)
- 并发 10 写入无冲突
- 万级数据量下搜索延迟 < 100ms

---
### Task 6: 集成测试 (Week 6)

**负责人**: OMO Agent + main  
**监督**: main  
**预计时间**: 3-5 天  

**子任务**:
```
6.1 长文本读写测试
    - 写入 5000 字文档
    - 读取并验证完整性
    - 验证切片准确性

6.2 语义搜索测试
    - 用不同关键词搜索
    - 验证搜索结果相关性
    - 验证元数据过滤

6.3 拼装质量测试
    - 人工评估拼装结果
    - 测试不同排序策略效果
    - 测试边界情况

6.4 OpenClaw/OpenCode 接入测试
    - 真实场景测试
    - 收集用户反馈
```

**交付物**:
- `tests/integration/test_phase2.py`
- `test_report_phase2.md`
- `benchmark_results_phase2.json`

**验收标准**:
- 所有集成测试用例 100% 通过
- 人工评估拼装质量 > 85% 可接受
- OpenClaw/OpenCode 能正常使用 Phase 2 功能

---
### Task 7: 文档更新 (Week 6)

**负责人**: main (主笔) + OMO Agent (辅助)  
**监督**: main  
**预计时间**: 2-3 天  

**交付物**:
- `docs/SLICING_STRATEGY.md` (切片策略说明)
- `docs/VECTOR_INTEGRATION.md` (向量库集成指南)
- `docs/ASSEMBLER.md` (拼装器使用指南)
- `docs/PERFORMANCE.md` (性能优化指南)
- 更新 `README.md` (添加 Phase 2 功能介绍)
- 更新 `docs/API.md` (添加新接口说明)

**验收标准**:
- 5 篇新文档完成
- 现有文档更新完成
- 文档示例可执行

---
## 四、技术选型

### 4.1 向量库

| 选项 | 优势 | 劣势 | 选择 |
|------|------|------|------|
| **ChromaDB** | 轻量级、本地模式、Python 原生 | 大规模性能一般 | ✅ **首选** |
| Qdrant | 功能强大、支持分布式 | 较重、需单独部署 | 备选 |
| FAISS | Facebook 开源、高性能 | API 较底层、需封装 | 备选 |

### 4.2 向量模型

| 选项 | 维度 | 中文支持 | 速度 | 选择 |
|------|------|---------|------|------|
| **BGE-M3** | 1024 | 优秀 | 中等 | ✅ **首选** |
| m3e-base | 768 | 优秀 | 快 | 备选 |
| text2vec | 512 | 良好 | 快 | 备选 |

### 4.3 切片策略

| 策略 | 适用场景 | 优先级 |
|------|---------|-------|
| **HybridSplitter** | 通用场景 | P0 |
| ParagraphSplitter | 结构化文档 | P1 |
| SemanticSplitter | 高语义密度文本 | P1 |

---
## 五、时间表

| Week | 任务 | 里程碑 |
|------|------|--------|
| **Week 1** | Task 1: 文本切片 | 切片器完成 + 测试通过 |
| **Week 2-3** | Task 2: 向量库集成 | ChromaDB 集成完成 |
| **Week 3** | Task 3: MFT 指针关联 | lcn_pointers 实现完成 |
| **Week 4-5** | Task 4: Assembler 拼装 | 拼装器完成 + 测试通过 |
| **Week 5** | Task 5: 性能优化 | LRU 缓存 + 并行优化完成 |
| **Week 6** | Task 6-7: 集成测试 + 文档 | Phase 2 发布准备 |

---
## 六、与 Phase 1 的差异

| 维度 | Phase 1 | Phase 2 |
|------|---------|---------|
| **存储** | SQLite (LIKE 搜索) | SQLite + ChromaDB |
| **切片** | 无 (整段存储) | 智能切片 (500-2000 字) |
| **检索** | 关键词匹配 | 语义检索 + 关键词 |
| **拼装** | 无 (直接返回) | 动态拼装 (去重 + 排序) |
| **复杂度** | 低 (MVP) | 中 (生产可用) |
| **代码量** | ~1300 行 | ~3000 行 (新增~1700 行) |

---
## 七、低价服务器适配方案（重要）

### 7.1 环境约束

| 约束 | 影响 | 级别 |
|------|------|------|
| **无 GPU** | 向量计算速度降低 10-50倍 | 🔴 高 |
| **2GB 内存** | 无法加载大型向量模型（BGE-M3约2.3GB） | 🔴 高 |
| **4GB 内存** | 可勉强加载，但剩余空间紧张 | 🟡 中 |

### 7.2 多模型/API 兼容架构（核心设计）

**设计目标**：同时支持本地模型 + 远程 API，自动适配不同环境，扩大支持范围。

#### 7.2.1 支持的向量源（全部兼容）

| 类型 | 名称 | 内存/成本 | 向量维度 | 适用场景 |
|------|------|----------|---------|---------|
| **本地模型** | m3e-small | ~200MB | 256 | 2GB环境、低延迟需求 |
| **本地模型** | m3e-base | ~800MB | 768 | 4GB环境、平衡性能 |
| **本地模型** | BGE-M3 | ~2.3GB | 1024 | >4GB环境、高质量向量 |
| **远程API** | 百度智能云 | ¥0.002/次 | 768 | 无本地资源、稳定国内网络 |
| **远程API** | 阿里云向量 | ¥0.001/次 | 768 | 无本地资源、性价比优先 |
| **远程API** | OpenAI Embedding | $0.0001/次 | 1536 | 国际环境、高质量向量 |

#### 7.2.2 统一配置管理

```yaml
# config.yaml - 向量源配置
embedding:
  # 首选方案（按优先级排序）
  sources:
    - name: m3e-base        # 首选：本地模型（平衡）
      type: local
      model: m3e-base
      dimension: 768
      memory_limit: 800MB
    
    - name: m3e-small       # 备选：本地模型（轻量）
      type: local
      model: m3e-small
      dimension: 256
      memory_limit: 200MB
    
    - name: baidu-api       # 备选：远程API
      type: remote
      api_url: https://cloud.baidu.com/embedding
      api_key: ${BAIDU_API_KEY}
      dimension: 768
    
    - name: aliyun-api      # 备选：远程API
      type: remote
      api_url: https://api.aliyun.com/embedding
      api_key: ${ALIYUN_API_KEY}
      dimension: 768
  
  # 自动切换规则
  auto_switch:
    enabled: true
    rules:
      - condition: "memory_available < 500MB"
        action: "switch_to m3e-small"
      
      - condition: "memory_available < 200MB"
        action: "switch_to baidu-api"
      
      - condition: "model_load_failed"
        action: "try_next_source"
      
      - condition: "api_timeout > 5s"
        action: "switch_to_local_model"
```

#### 7.2.3 自动适配逻辑

```python
# mfs/embedding.py - 多源兼容实现
class MultiSourceEmbedding:
    """
    同时兼容多个本地模型 + 远程 API
    自动检测环境并选择最优方案
    """
    
    def __init__(self, config):
        self.sources = config['embedding']['sources']
        self.auto_switch = config['embedding']['auto_switch']
        self.current_source = None
        self.load_priority()
    
    def load_priority(self):
        """按优先级依次尝试加载"""
        for source in self.sources:
            if self.can_load(source):
                self.current_source = self.init_source(source)
                return True
        raise RuntimeError("所有向量源均不可用")
    
    def can_load(self, source):
        """检测是否可以加载该向量源"""
        if source['type'] == 'local':
            # 检测内存是否足够
            available = get_available_memory()
            return available >= source['memory_limit']
        elif source['type'] == 'remote':
            # 检测网络连通性
            return check_api_available(source['api_url'])
        return False
    
    def encode(self, texts):
        """向量化文本（自动切换）"""
        try:
            return self.current_source.encode(texts)
        except Exception as e:
            # 自动切换到下一个可用源
            self.switch_to_next()
            return self.current_source.encode(texts)
    
    def switch_to_next(self):
        """切换到下一个可用的向量源"""
        current_idx = self.sources.index(self.current_source.config)
        for i in range(current_idx + 1, len(self.sources)):
            if self.can_load(self.sources[i]):
                self.current_source = self.init_source(self.sources[i])
                log.warning(f"向量源已切换: {self.current_source.name}")
                return
        raise RuntimeError("无可用的备用向量源")
```

#### 7.2.4 环境自动检测

| 检测项 | 检测方法 | 决策逻辑 |
|--------|---------|---------|
| **可用内存** | `psutil.virtual_memory().available` | < 200MB → API；< 800MB → m3e-small；> 800MB → m3e-base |
| **GPU 状态** | `torch.cuda.is_available()` | 有GPU → 加速向量化；无GPU → CPU计算 |
| **网络连通** | `ping API服务器` | API可用 → 远程向量；不可用 → 本地模型 |
| **调用频率** | 统计最近1小时调用量 | > 10000次 → 本地模型（成本优化） |

### 7.3 内存管理策略

- **延迟加载**：启动时不加载模型，首次使用时按需加载
- **内存释放**：长时间未使用时释放模型内存（gc.collect，30分钟阈值）
- **批量处理**：减少模型加载次数，批量编码（batch_size=32）
- **模型卸载**：内存紧张时自动卸载大模型，切换到轻量模型或API

### 7.4 ChromaDB 内存优化

- 使用 `duckdb+parquet` 磁盘存储模式（节省 ~70% 内存）
- 禁用匿名遥测数据
- 持久化目录设置
- 支持向量维度动态适配（256/768/1024）

### 7.5 无 GPU 性能预估

| 操作 | GPU 环境 | CPU 环境 (2核) | 远程 API | 延迟对比 |
|------|---------|---------------|---------|---------|
| 单文本向量化 (512维) | ~5ms | ~50-200ms | ~100-500ms | 10-40倍 |
| 批量向量化 (32条) | ~50ms | ~500-2000ms | ~300-1500ms | 10-40倍 |
| 向量搜索 (1000条) | ~5ms | ~10-30ms | ~20-50ms | 2-6倍 |

**性能优先级**：GPU 本地 > CPU 本地 > 远程 API

### 7.6 验收标准调整（低价服务器）

| 指标 | 原目标值 | 低价服务器调整值 | 远程 API 模式 |
|------|---------|----------------|-------------|
| 写入延迟 | <500ms | <1000ms（CPU本地） | <1500ms |
| 向量搜索延迟 | <50ms | <100ms（CPU本地） | <150ms |
| 内存占用 | 无限制 | <1GB（模型+ChromaDB） | <100MB |
| 向量源切换时间 | - | <5秒 | <3秒 |

### 7.7 兼容性矩阵（完整支持范围）

| 环境 | 首选方案 | 备选方案 | 降级方案 |
|------|---------|---------|---------|
| **高端服务器（>8GB + GPU）** | BGE-M3 (GPU加速) | m3e-base | 百度API |
| **中端服务器（4-8GB）** | m3e-base | m3e-small | 阿里云API |
| **低端服务器（2-4GB）** | m3e-small | 百度API | OpenAI API |
| **极低资源（<2GB）** | 百度API | 阿里云API | 无本地模型 |
| **无网络环境** | m3e-small | m3e-base | 无API |

---
## 八、风险管理

### 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|-----|------|---------|
| 向量模型选择不当 | 中 | 中 | 预留模型切换接口，支持热切换 |
| ChromaDB 性能瓶颈 | 低 | 中 | 预留 Qdrant 切换方案 |
| 拼装算法复杂度高 | 中 | 中 | 先实现基础版，后续优化 |
| 切片策略不匹配场景 | 中 | 中 | 支持多种策略 + 用户自定义 |

### 进度风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|-----|------|---------|
| 向量库集成延期 | 中 | 中 | 提前调研文档，准备 Demo |
| 性能优化不达标 | 中 | 中 | 预留缓冲时间，分阶段优化 |
| 集成测试发现问题多 | 高 | 中 | 提前进行小规模测试 |

---
## 九、交付物清单

### 代码 (~3000 行)

| 文件 | 说明 | 行数预估 |
|------|------|---------|
| `mfs/slicers/*.py` | 切片器模块 | ~500 |
| `mfs/vector_store.py` | 向量库封装 | ~300 |
| `mfs/embedding.py` | 向量模型封装 | ~200 |
| `mfs/chunk_manager.py` | Chunk 管理 | ~250 |
| `mfs/assembler.py` | 拼装器 | ~400 |
| `mfs/cache.py` | LRU 缓存 | ~200 |
| `tests/test_*.py` | 测试文件 | ~800 |
| `benchmarks/*.py` | 性能测试 | ~150 |
| **新增总计** | | **~2800 行** |

### 文档

| 文件 | 说明 |
|------|------|
| `docs/SLICING_STRATEGY.md` | 切片策略说明 |
| `docs/VECTOR_INTEGRATION.md` | 向量库集成指南 |
| `docs/ASSEMBLER.md` | 拼装器使用指南 |
| `docs/PERFORMANCE.md` | 性能优化指南 |
| `README.md` (更新) | 添加 Phase 2 功能 |
| `docs/API.md` (更新) | 添加新接口说明 |

---
## 十、关键决策点

| 日期 | 决策内容 | 决策人 | 状态 |
|------|---------|-------|------|
| Day 1 | 切片策略确认 | 用户 | ⏳ 待确认 |
| Day 1 | 向量库/模型选型确认 | 用户 | ⏳ 待确认 |
| Week 3 | MFT 指针设计审查 | 用户 | ⏳ 待确认 |
| Week 5 | 性能优化方案审查 | 用户 | ⏳ 待确认 |
| Week 6 | Phase 2 发布审查 | 用户 | ⏳ 待确认 |

---
## 十一、附录：命令速查

```bash
# 安装 Phase 2 依赖
pip install chromadb sentence-transformers

# 运行切片器测试
pytest tests/test_slicers.py -v

# 运行向量库测试
pytest tests/test_vector_store.py -v

# 运行性能基准测试
python benchmarks/benchmark_phase2.py

# 生成覆盖率报告
pytest --cov=mfs --cov-report=html --cov-fail-under=80
```

---
**文档版本**: v1.0  
**创建时间**: 2026-04-14  
**维护人**: main
